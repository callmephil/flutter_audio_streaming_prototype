{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile web debug",
            "command": "flutter run -d chrome --web-renderer canvaskit --web-browser-flag '--disable-web-security' -t lib/main.dart --release",
            "type": "shell"
        }
    ]
}
